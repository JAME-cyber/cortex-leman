# OS
.DS_Store
Thumbs.db

# Config with potential secrets
.form-config

# Scripts (not needed for static deploy)
configure-form.sh
update-formspree-url.sh
test-form.sh
quick-deploy.sh
serve.sh
start.sh

# Docker (not needed for static deploy)
Dockerfile
nginx.conf

# Sensitive data
sankofa/config/commenters_db.json
*.secret
*.key
client_secret*.json
broadcast.goauth

# Backend (not needed for static deploy)
backend/
