# OS
.DS_Store
Thumbs.db

# Config with potential secrets
.form-config

# Scripts (not needed for static deploy)
configure-form.sh
update-formspree-url.sh
test-form.sh
quick-deploy.sh
serve.sh
start.sh

# Docker (not needed for static deploy)
Dockerfile
nginx.conf

# Docs (not needed for static deploy)
DEPLOYMENT-READY.md
FORMSPREE-SETUP.md
FORMULAIRE-CONFIG.md
FORMULAIRE-README.md
README.md

# Backend (not needed for static deploy)
backend/
